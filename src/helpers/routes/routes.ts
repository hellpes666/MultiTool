// store the Route values in enum type
enum Route {
  Weather = 'Weather',
  Movies = 'Movies',
  Blog = 'Blog',
  Friends = 'Friends',
}

// Used in Header
export const ROUTES: Route[] = [
  Route.Weather,
  Route.Movies,
  Route.Blog,
  Route.Friends,
];
