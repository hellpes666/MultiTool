// store the Route values in enum type
enum Route {
  Weather = 'Weather',
  Cinema = 'Cinema',
  Blog = 'Blog',
  Friends = 'Friends',
}

// Used in Header
export const ROUTES: Route[] = [
  Route.Weather,
  Route.Cinema,
  Route.Blog,
  Route.Friends,
];
